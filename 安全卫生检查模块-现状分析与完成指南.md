# 安全卫生检查模块 — 现状分析与完成指南

**版本：** V3.0
**日期：** 2026-06-13
**作者：** 吴晓峰
**状态：** 开发完成，全部功能可用

---

## 1. 需求覆盖（来自需求分析文档 §3.7）

| 编号 | 需求描述 | 优先级 | 状态 |
|------|----------|:--:|:--:|
| SC-001 | 制定检查计划（时间/人员/范围） | 高 | ✅ |
| SC-002 | 移动端检查打分 | 高 | ✅ |
| SC-003 | 检查项自定义配置 | 中 | ✅ |
| SC-004 | 拍照上传 | 中 | ❌ 已移除 |
| SC-005 | 自动汇总检查结果，生成检查报告 | 高 | ✅ |
| SC-006 | 整改跟踪 | 中 | ✅ |
| SC-007 | 数据统计和分析（多维度） | 中 | ✅ |

---

## 2. 模块文件清单

### 后端（9 个文件，31 个 API 接口）

```
controller/
  InspectionPlanController.java    — 13 接口（CRUD + 分页 + start/complete/cancel + 状态流转校验）
  InspectionRecordController.java  — 12 接口（CRUD + 分页 + search + rectify/approve）
  InspectionItemController.java    —  7 接口（CRUD + 分页 + active/category）
  UploadController.java            —  2 接口（单文件/批量上传）
service/
  InspectionPlanService.java       — 状态流转校验 + 生命周期方法
  InspectionRecordService.java     — 整改流程校验 + need_rectification 自动设置
  InspectionItemService.java       — 分页 CRUD
mapper/
  InspectionPlanMapper.java        — MyBatis 注解 + 分页 + count
  InspectionRecordMapper.java      — MyBatis 注解 + 分页 + count + 动态 SQL search
config/
  WebMvcConfig.java                — uploads 目录静态资源映射
```

### 前端（5 个页面，32 个 API 函数）

| 页面 | 功能 | 亮点 |
|------|------|------|
| `InspectionPlanList.vue` | 计划 CRUD + 状态流转 | 4 张 StatCard + 进度条 + 表格优化 |
| `InspectionExecute.vue` | 执行检查打分 | 批量多房间 + PASS/FAIL 按钮组 + 吸底提交栏 |
| `InspectionRecordList.vue` | 整改跟踪管理 | 楼栋筛选 + 整改弹窗 + 验证操作 |
| `InspectionItemList.vue` | 检查项配置 | 自定义 Tag + 文字链接操作 |
| `InspectionStatistics.vue` | 统计仪表盘 | 6 个 ECharts 图表 + CSV 导出 + 月度最佳宿舍 |

### 关联页面

| 页面 | 关联内容 |
|------|---------|
| `StudentRoom.vue` | 学生端"安全检查记录"卡片 |
| `StudentLayout.vue` | 整改通知推送至学生通知铃铛 |
| `Dashboard.vue` | 本月检查概览统计卡片 |

---

## 3. API 接口清单

### 计划 `/api/inspection/plans`
```
GET    /                       分页列表 (?page=1&size=10)
GET    /pending                待执行
GET    /status/{status}        按状态筛选
GET    /type/{type}            按类型筛选
GET    /{id}                   详情
POST   /                       创建
PUT    /{id}                   更新（不含 status）
PUT    /{id}/status            更新状态（含流转校验）
POST   /{id}/start             开始执行
POST   /{id}/complete          完成
POST   /{id}/cancel            取消
DELETE /{id}                   删除
```

### 记录 `/api/inspection/records`
```
GET    /                       分页列表
GET    /pending                待整改
GET    /plan/{planId}          按计划
GET    /room/{roomId}          按房间
GET    /status/{status}        按整改状态
GET    /result/{result}        按结果
GET    /search                 多条件搜索（日期/楼栋/状态）
GET    /{id}                   详情
POST   /                       创建（自动设置整改字段）
PUT    /{id}                   更新
POST   /{id}/rectify           提交整改（PENDING→COMPLETED）
POST   /{id}/approve           审核整改（COMPLETED→VERIFIED）
DELETE /{id}                   删除
```

### 检查项 `/api/inspection/items`
```
GET    /                       分页列表
GET    /active                 启用项
GET    /category/{cat}         按类别
GET    /{id}                   详情
POST   /                       创建
PUT    /{id}                   更新
DELETE /{id}                   删除
```

### 文件上传 `/api/upload`
```
POST   /                       单文件
POST   /batch                  批量
```

---

## 4. 状态流转规则

### 计划
```
DRAFT → SCHEDULED → IN_PROGRESS → COMPLETED
  │        │             │
  └────────┴─────────────┴──→ CANCELLED
```

### 整改
```
FAIL → PENDING → COMPLETED → VERIFIED
PASS → NONE
```

---

## 5. 新增功能（V3.0）

| 功能 | 位置 | 说明 |
|------|------|------|
| **批量检查** | InspectionExecute | 房间多选，提交时循环创建记录 |
| **CSV 导出** | InspectionStatistics | 一键导出检查记录为 CSV |
| **月度最佳宿舍** | InspectionStatistics | 当月评分 Top 10 排名卡片 |
| **学生端检查记录** | StudentRoom | 查看自己房间的检查结果和整改状态 |
| **整改通知** | StudentLayout | 学生通知铃铛推送待整改提醒 |
| **Dashboard 概览** | Dashboard | 工作台本月检查统计卡片 |
| **吸底提交栏** | InspectionExecute | 进度条 + 总分大字 + sticky footer |
| **PASS/FAIL 按钮组** | InspectionExecute | 移除手动输入，自动算分（合格=满分，不合格=0） |
| **不合格强制反馈** | InspectionExecute | 不合格项必须填写扣分原因方可提交 |

---

## 6. 前端全局优化

| 项目 | 文件数 | 说明 |
|------|:--:|------|
| CSS 变量系统 | 6 | `styles/`（variables, element-overrides, reset, page-layout, auth-card, layout-sidebar） |
| 共享组件 | 6 | DataTable, FormDialog, EmptyState, SkeletonLoader, StatCard, EChartsWrapper |
| Composables | 5 | useTable, useForm, useAuth, useConfirm, useDebounce |
| 常量 | 1 | status.js（10 组状态映射） |
| 重构页面 | 13 | Layout/Login/Register/Dashboard/Building/Room/Inspection*5 |

---

## 7. Bug 修复记录

| Bug | 原因 | 修复 |
|-----|------|------|
| 编辑计划后状态丢失 | update SQL 含 status 列 | 从 update SQL 移除 status |
| 执行检查无法选房间 | rooms API 返回格式解析错误 | 改用 /rooms/building/{id} |
| 登录后反复跳转登录页 | 403 拦截器过于激进 | 仅 401 触发跳转，403 仅 console 警告 |
| 学生端 403 报错 | 调用了不存在的端点 | /checkin/today→/checkin/date, /students/roommates→移除 |
| 统计页日期筛选无效 | 用了无日期参数的 getAll | 改用 searchRecords 传 startDate/endDate |
| 共享 CSS 未加载 | main.js 遗漏导入 | 补充 page-layout/layout-sidebar/auth-card 导入 |
| Dashboard 日期越界 | 硬编码 endDate=31 | 改为动态取当月最后一天 |
| 布局右侧大片留白 | layout-sidebar.css 未导入 | main.js 补充导入 |

---

## 8. 测试账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | `admin` | `admin123` |
| 学生 | `2024001` | `123456` |

## 9. 运行命令

```bash
# 后端
cd backend
mvn spring-boot:run

# 前端
cd frontend
npm install
npm run dev
```

访问 `http://localhost:5173`

---

**文档结束**
